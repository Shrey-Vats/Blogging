export interface blog {
    title: string,
    content: string,
    image: string,
    slug?: string
}

export interface comment {
    title: string,
    description: string,
    rating: number,
    
}